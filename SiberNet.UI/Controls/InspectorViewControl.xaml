<UserControl x:Class="SiberNet.UI.Controls.InspectorViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SiberNet.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
            DataContext="{Binding InspectorViewModel, Source={StaticResource ComponentHub}}" >
    <ContentControl Style="{StaticResource CyberpunkImageChaildStyle}" Padding="6" Background="#FF111318">
        <ContentControl.Tag>
            <CornerRadius>6</CornerRadius>
        </ContentControl.Tag>

        <Grid>
            <Border BorderThickness="1" BorderBrush="#33FFB300" CornerRadius="4">
                <!-- Просто биндимся на свойство текста отчёта -->
                <TextBox Text="{Binding ReportText, Mode=OneWay}"
                         FontFamily="Consolas"
                         FontSize="13"
                         Background="#FF111318"
                         Foreground="#00FFCC"
                         BorderThickness="0"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="NoWrap"
                         HorizontalScrollBarVisibility="Auto"
                         VerticalScrollBarVisibility="Auto"
                         Padding="10"/>
            </Border>
        </Grid>
    </ContentControl>
</UserControl>
