<UserControl x:Class="WpfHomeNet.Controls.RegistrationView.RegistrationViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfHomeNet.Controls.RegistrationView"
             xmlns:converters="clr-namespace:WpfHomeNet.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <converters:SecureStringToStringConverter x:Key="SecureStringConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Email -->
        <Label Grid.Row="0" Grid.Column="0" Content="Email:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" 
                 Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5" Padding="5"/>

        <!-- Password -->
        <Label Grid.Row="1" Grid.Column="0" Content="Пароль:" VerticalAlignment="Center"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" 
             Password="{Binding Password, 
                                Converter={StaticResource SecureStringConverter}, 
                                UpdateSourceTrigger=PropertyChanged}"
             Margin="5" Padding="5"/>

        <!-- User Name -->
        <Label Grid.Row="2" Grid.Column="0" Content="Имя пользователя:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" 
                 Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5" Padding="5"/>

        <!-- Phone -->
        <Label Grid.Row="3" Grid.Column="0" Content="Телефон:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" 
                 Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5" Padding="5"/>

        <!-- Register Button -->
        <Button Grid.Row="4" Grid.ColumnSpan="2" 
               Content="Зарегистрироваться" 
               Command="{Binding RegisterCommand}"
               Margin="5" Padding="10"
               HorizontalAlignment="Center"/>

        <!-- Error Message -->
        <Label Grid.Row="5" Grid.ColumnSpan="2"
               Content="{Binding ErrorMessage}"
               Margin="5"
               HorizontalAlignment="Center">
            <Label.Style>
                <Style TargetType="{x:Type Label}">
                    <Setter Property="Foreground" Value="Black"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ValidationState}" Value="Error">
                            <Setter Property="Foreground" Value="Red"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ValidationState}" Value="Success">
                            <Setter Property="Foreground" Value="Green"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Label.Style>
        </Label>
    </Grid>
</UserControl>
